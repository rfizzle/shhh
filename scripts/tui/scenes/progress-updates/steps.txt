# program test: TestProgram_ASilentRunIsAskedForItsStatus,
#   TestProgram_ARunningCommandTailsIntoItsRow,
#   TestProgram_TheQuitChordArmsThenLeaves
#
# The second sitting is a second process opening the conversation again,
# which no program test can be.
#
# A scripted silent investigation crosses the default checkpoint threshold, so
# the scene holds the two states the public status has to be read in: the note
# on its own under the reader's prompt and over the calls it explains, and the
# note beside a command that is still running.
#
# What the captures are read for is the rung, which cells cannot carry:
# 03-status against the Main artboard, and 05-running for the note staying
# under the command rather than over it. The `.txt` says where the words are;
# the picture beside it says how loud they are.
setup for i in 01 02 03 04 05 06 07 08 09 10 11 12; do printf 'package fixture\n' > "round${i}.go"; done; printf 'package fixture\n' > stream.go
snap 01-start "Some things worth doing first"
keys "trace the checkpoint" Enter
snap 02-silent-run "12 reads"
snap 03-status "the round limit is counted"
snap 04-card "[y] run it once"
keys y
snap 05-running "running…"
snap 06-settled "the run is quiet again"
keys C-c
snap 07-quit-armed "again quits"
keys C-c
snap 08-exit "that is everything the screen was holding"
# The second sitting opens the same conversation again (see `launch`). What
# the capture is read for is the note's rung after the rebuild: the transcript
# is put back from the stored messages alone, so a mark that lived only on the
# frame would come back as an answer at an answer's weight. Wait for the
# resume row, which is the proof the second process is the one on screen.
snap 09-reopened "resumed master"
