[[row]]
name = "a trim leaves the result the current turn is working from"
why = "the turn being answered is what the model is acting on; a trim that reached into it would take back the result it just read"
expect = ["kept"]
window = 1200
summary = "The session read the importer and the exporter. The importer flushes on close; the exporter writes a header and one line per record. What is left is the partial-batch case and the tests around it."
needs = [
  "NEWEST-RESULT",
]
conversation = [
  "system: You are shhh, a coding agent working in a checkout.",
  "user: find out why the importer drops the last row",
  "assistant: reading the importer.",
  "tool: OLDEST-RESULT: internal/import/importer.go — the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close.",
  "user: and check what the exporter does with a partial batch",
  "assistant: reading the exporter.",
  "tool: NEWEST-RESULT: internal/export/exporter.go — the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record.",
]

[[row]]
name = "a trim spends the results of turns already answered"
why = "this is what a trim costs, and it is only affordable because those results were consumed rounds ago"
expect = ["lost"]
window = 1200
summary = "The session read the importer and the exporter. The importer flushes on close; the exporter writes a header and one line per record. What is left is the partial-batch case and the tests around it."
needs = [
  "OLDEST-RESULT",
]
conversation = [
  "system: You are shhh, a coding agent working in a checkout.",
  "user: find out why the importer drops the last row",
  "assistant: reading the importer.",
  "tool: OLDEST-RESULT: internal/import/importer.go — the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close.",
  "user: and check what the exporter does with a partial batch",
  "assistant: reading the exporter.",
  "tool: NEWEST-RESULT: internal/export/exporter.go — the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record.",
]

[[row]]
name = "a conversation under the line is left alone"
why = "recovery is what a full window earns; a step that ran anyway would spend the provider's cached prefix every round"
expect = ["untouched"]
window = 200000
summary = "The session read the importer and the exporter. The importer flushes on close; the exporter writes a header and one line per record. What is left is the partial-batch case and the tests around it."
needs = [
  "NEWEST-RESULT",
  "OLDEST-RESULT",
]
conversation = [
  "system: You are shhh, a coding agent working in a checkout.",
  "user: find out why the importer drops the last row",
  "assistant: reading the importer.",
  "tool: OLDEST-RESULT: internal/import/importer.go — the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close. the importer reads rows in batches of 500 and flushes on close.",
  "user: and check what the exporter does with a partial batch",
  "assistant: reading the exporter.",
  "tool: NEWEST-RESULT: internal/export/exporter.go — the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record. the exporter writes a header row and then one line per record.",
]

[[row]]
name = "a conversation a trim cannot shrink is rebuilt around the current turn"
why = "the summary describes what was cut, and the turn in the middle of being answered is the one place a description is not good enough"
expect = ["kept"]
window = 1600
summary = "The session read the importer and the exporter. The importer flushes on close; the exporter writes a header and one line per record. What is left is the partial-batch case and the tests around it."
needs = [
  "Summary of the conversation so far",
  "now say what you would change",
  "NEWEST-RESULT",
]
conversation = [
  "system: You are shhh, a coding agent working in a checkout.",
  "user: work out why the importer drops the last row and write down what you find",
  "assistant: The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch.",
  "assistant: The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch.",
  "user: now say what you would change",
  "assistant: reading the exporter one more time.",
  "tool: NEWEST-RESULT: internal/export/exporter.go — the flush happens before the last append.",
]

[[row]]
name = "the rebuilt conversation still opens with the system prompt"
why = "a rebuild that dropped it would leave the session running with no instructions at all, and nothing would say so"
expect = ["kept"]
window = 1600
summary = "The session read the importer and the exporter. The importer flushes on close; the exporter writes a header and one line per record. What is left is the partial-batch case and the tests around it."
needs = [
  "You are shhh, a coding agent working in a checkout.",
]
conversation = [
  "system: You are shhh, a coding agent working in a checkout.",
  "user: work out why the importer drops the last row and write down what you find",
  "assistant: The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch.",
  "assistant: The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch. The session read the loader, the parser and the two writers, and wrote down what each of them does with a partial batch.",
  "user: now say what you would change",
  "assistant: reading the exporter one more time.",
  "tool: NEWEST-RESULT: internal/export/exporter.go — the flush happens before the last append.",
]
