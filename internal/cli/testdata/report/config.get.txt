  shhh config get — provider.model

  ✓ gpt-4o  [user]
      The model every surface runs on where its own key — `cmd_model`,
      `chat_model` or `code_model` — names none.
      Unset it and (the provider's own default) stands.
      --model on the command line, then SHHH_MODEL in the environment, outrank
      this.
